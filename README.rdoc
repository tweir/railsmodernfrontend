== Overview
Rails plus a 'modern' frontend asset pipeline that is not dependent on sprockets

== Install

npm install -g bower
npm install -g gulp

bundle install
npm install
bower install

gulp build

== References
* https://github.com/jessemartin/boilerplate
